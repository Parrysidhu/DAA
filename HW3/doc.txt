#include<stdio.h>

/*int selectionsort(int arr[],int n){
    int i,x,j,temp;
    for (i=0;i<n-2;i++){
        x=i;
        for(j=i+1;j<=n;j++){
            if(arr[j] < arr[x]){
                x=j;
            }
        }
        temp= arr[x];
        arr[x]= arr[i];
        arr[i]= temp;

    }
    for(i=1;i<=n;i++){
        printf("%d", arr[i]);
    }

}*/




int main(){
	int n,k,val;
	int arr[n];
	printf("input size of array: ");
	scanf("%d",&n);
	printf("input values of array");

    return 0;
	//for(k=0; k<n; k++){
       // scanf("%d",&val);
        //arr[k]=val;
        //}


	//selectionsort(arr,n);

}


